{
  "type": "array",
  "items": {
    "$ref": "#/$defs/asset"
  },
  "minItems": 1,
  "$defs": {
    "asset": {
      "type": "object",
      "properties": {
        "protocol": {
          "type": "string",
          "description": "The protocol name used for displaying purposes only"
        },
        "denom": {
          "type": "string",
          "description": "The denom of an asset"
        },
        "symbol": {
          "type": "string",
          "description": "The symbol of an asset. eg: ORO"
        },
        "exponent": {
          "type": "integer"
        },
        "uri": {
          "type": "string",
          "description": "The logo of the token"
        },
        "token_type": {
          "type": "string",
          "description": "The type of the token. eg: native, cw20"
        },
        "coingeckoId": {
          "type": "string",
          "description": "coingecko id"
        },
        "originDenom": {
          "type": "string",
          "description": "origin denom of the token"
        },
        "originChainId": {
          "type": "string",
          "description": "origin ChainId of the token"
        }
      },
      "required": ["protocol", "symbol", "denom", "exponent"],
      "additionalProperties": false
    }
  }
}
